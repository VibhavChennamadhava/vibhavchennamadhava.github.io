---
const currentYear = new Date().getFullYear();

const links = [
  { href: '/projects', label: 'Projects' },
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Contact' },
  { href: '/resume.pdf', label: 'Resume' },
];

const socials = [
  { href: 'https://github.com/VibhavChennamadhava', icon: 'github', label: 'GitHub' },
  { href: 'https://www.linkedin.com/in/vibhav-chennamadhava-61184b190', icon: 'linkedin', label: 'LinkedIn' },
  { href: 'mailto:vibhav.chennamadhava@gmail.com', icon: 'mail', label: 'Email' },
];
---

<footer class="bg-bg-secondary border-t border-border-default">
  <div class="container py-12">
    <div class="flex flex-col lg:flex-row items-start justify-between gap-10">
      <div class="max-w-md">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-10 h-10 rounded-lg bg-bg-hover border border-border-default flex items-center justify-center font-extrabold text-text-primary tracking-tight">
            VC
          </div>
          <div>
            <div class="text-lg font-bold text-text-primary">Sri Vibhav Raju Chennamadhava</div>
            <div class="text-sm text-text-muted">Cybersecurity • Cloud Security • SOC</div>
          </div>
        </div>
        <p class="text-text-secondary">
          Security operations and cloud/IAM focused — incident triage, log analysis, vulnerability management, and clean evidence-ready documentation.
        </p>
      </div>

      <div class="grid grid-cols-2 sm:grid-cols-3 gap-10">
        <div>
          <h3 class="text-xs font-semibold uppercase tracking-wider text-text-muted mb-4">Site</h3>
          <ul class="space-y-3">
            {links.map((link) => (
              <li>
                <a href={link.href} class="text-text-secondary hover:text-text-primary transition-colors duration-200">
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <h3 class="text-xs font-semibold uppercase tracking-wider text-text-muted mb-4">Connect</h3>
          <ul class="space-y-3">
            {socials.map((link) => (
              <li>
                <a
                  href={link.href}
                  target={link.href.startsWith('mailto:') ? undefined : '_blank'}
                  rel={link.href.startsWith('mailto:') ? undefined : 'noopener noreferrer'}
                  class="text-text-secondary hover:text-text-primary transition-colors duration-200 inline-flex items-center gap-2"
                >
                  <i data-lucide={link.icon} class="w-4 h-4"></i>
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>

        <div class="hidden sm:block">
          <h3 class="text-xs font-semibold uppercase tracking-wider text-text-muted mb-4">Availability</h3>
          <p class="text-text-secondary text-sm">Open to security analyst / SOC roles and cloud security opportunities.</p>
        </div>
      </div>
    </div>

    <div class="pt-8 mt-10 border-t border-border-default flex flex-col sm:flex-row items-center justify-between gap-4">
      <p class="text-text-muted text-sm">&copy; {currentYear} Vibhav Chennamadhava.</p>

      <div class="flex items-center gap-3">
        {socials.map((link) => (
          <a
            href={link.href}
            target={link.href.startsWith('mailto:') ? undefined : '_blank'}
            rel={link.href.startsWith('mailto:') ? undefined : 'noopener noreferrer'}
            class="w-10 h-10 rounded-full flex items-center justify-center text-text-muted hover:text-accent-teal hover:bg-bg-hover transition-all duration-200"
            aria-label={link.label}
          >
            <i data-lucide={link.icon} class="w-5 h-5"></i>
          </a>
        ))}
      </div>
    </div>
  </div>
</footer>
